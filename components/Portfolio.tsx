'use client';
import { useEffect, useState, type ReactNode } from 'react';
import { motion, useReducedMotion } from 'framer-motion';
import { ArrowDown, ArrowUpRight, ArrowRight, Github, Linkedin, Mail, FileText, Compass, Code2, Network, Layers3, GraduationCap, Menu, X, MapPin, Map, Database, Cloud } from 'lucide-react';
import { profile, navigation, experiences, projects, skills, education } from '@/lib/portfolio';
import ArtifactDisplay from './ArtifactDisplay';
import CursorTrail from './CursorTrail';
function Reveal({ children, className = '' }: { children: ReactNode; className?: string }) {
  const reduced = useReducedMotion();
  return <motion.div className={className} initial={{ opacity: 1, y: reduced ? 0 : 14 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, margin: '-35px' }} transition={{ duration: reduced ? 0 : 0.6 }}>{children}</motion.div>;
}
function SectionHeading({ number, title, subtitle }: { number: string; title: string; subtitle: string }) {
  return <div className="section-heading"><div><span className="eyebrow"><span className="chapter">{number}</span> {subtitle}</span><h2>{title}<span>.</span></h2></div><div className="heading-rule"><Map size={15} /></div></div>;
}
export default function Portfolio() {
  const [active, setActive] = useState('');
  const [menu, setMenu] = useState(false);
  useEffect(() => {
    const observer = new IntersectionObserver(entries => { for (const entry of entries) if (entry.isIntersecting) setActive(entry.target.id); }, { rootMargin: '-15% 0px -55% 0px', threshold: 0 });
    document.querySelectorAll('main section[id]').forEach(section => observer.observe(section));
    return () => observer.disconnect();
  }, []);
  return <><CursorTrail/><a href="#main" className="skip-link">Skip to content</a><header className="site-header"><nav className="container nav" aria-label="Main navigation"><a className="brand" href="#home" aria-label="Nicholas McCracken home"><Compass size={26} strokeWidth={1} /><span>NM<span className="brand-dot">.</span></span></a><div className="desktop-nav">{navigation.map(item=><a key={item} href={`#${item.toLowerCase()}`} aria-current={active===item.toLowerCase()?'location':undefined} className={active===item.toLowerCase()?'active':''}>{item}</a>)}</div><a className="nav-contact" href={profile.email}>Let’s connect <ArrowUpRight size={15}/></a><button className="menu-button" aria-label={menu?'Close navigation':'Open navigation'} aria-expanded={menu} aria-controls="mobile-nav" onClick={()=>setMenu(!menu)}>{menu?<X/>:<Menu/>}</button></nav>{menu&&<nav id="mobile-nav" className="mobile-nav" aria-label="Mobile navigation">{navigation.map(item=><a key={item} href={`#${item.toLowerCase()}`} onClick={()=>setMenu(false)}>{item}<ArrowUpRight size={16}/></a>)}</nav>}</header>
  <main id="main"><section className="hero container" id="home"><div className="hero-copy"><h1>Nicholas<br/><span>McCracken</span><span className="name-period">.</span></h1><p className="hero-role">{profile.title}</p><p className="hero-description">{profile.description}</p><div className="hero-actions"><a className="button primary" href="#projects">View Projects <ArrowDown size={16}/></a><a className="button secondary" href={profile.resume} target="_blank" rel="noreferrer"><FileText size={16}/> Resume <ArrowUpRight size={14}/></a></div><div className="social-links"><a href={profile.github} target="_blank" rel="noreferrer"><Github size={16}/> GitHub <ArrowUpRight size={12}/></a><a href={profile.linkedin} target="_blank" rel="noreferrer"><Linkedin size={16}/> LinkedIn <ArrowUpRight size={12}/></a><a href={profile.email}><Mail size={16}/> Email <ArrowUpRight size={12}/></a></div></div><ArtifactDisplay/><div className="hero-bottom"><span><MapPin size={13}/> {profile.status}</span><a href="#experience">SCROLL TO EXPLORE <ArrowDown size={14}/></a></div></section>
  <section tabIndex={-1} id="experience" className="section container"><SectionHeading number="01" title="Experience" subtitle="PAST ADVENTURES"/><div className="timeline">{experiences.map((job,index)=><Reveal className="timeline-item" key={job.company+job.role}><div className="timeline-meta"><span className="timeline-marker">{index===0?<span/>:null}</span><p>{job.date}</p><span className="eyebrow">{job.kind}</span></div><div className="timeline-content"><h3>{job.company}</h3><p className="job-role">{job.role}</p><p className="job-location"><MapPin size={13}/>{job.location}</p><ul>{job.points.map(point=><li key={point}>{point}</li>)}</ul></div></Reveal>)}</div></section>
  <section tabIndex={-1} id="projects" className="section projects-section"><div className="container"><SectionHeading number="02" title="Projects" subtitle="FEATURED QUESTS"/><div className="projects-grid">{projects.map(project=><Reveal className="project-card" key={project.number}><div className="project-art-header"><span>ARTIFACT {project.number}</span><span className="project-icon" aria-hidden="true">{project.icon}</span></div><div className="project-body"><span className="eyebrow">{project.type}</span><h3>{project.title}</h3><p>{project.description}</p>{project.highlight && <div className="project-highlight">{project.highlight}</div>}<div className="tags">{project.tags.map(tag=><span key={tag}>{tag}</span>)}</div><div className="project-footer"><a href={project.github} target="_blank" rel="noreferrer" aria-label={`View ${project.title} on GitHub`}><Github size={15}/> View repository <ArrowUpRight size={15}/></a></div></div></Reveal>)}</div></div></section>
  <section tabIndex={-1} id="education" className="section container"><SectionHeading number="03" title="Education" subtitle="TRAINING & STUDY"/><Reveal className="education-panel"><div className="university"><GraduationCap size={32} strokeWidth={1}/><h3>{education[0].university}</h3><span><MapPin size={13}/> {education[0].location}</span></div><div className="degrees">{education.map((degree,index)=><div className="degree" key={degree.degree}><span className="eyebrow">{index===0?'GRADUATE STUDIES':'UNDERGRADUATE STUDIES'}</span><h4>{degree.degree}</h4><p>{degree.date}</p><p>GPA: {degree.gpa}</p></div>)}</div></Reveal></section>
  <section tabIndex={-1} id="skills" className="section container"><SectionHeading number="04" title="Technical Toolkit" subtitle="TOOLS & DISCIPLINES"/><div className="skills-codex">{skills.map((category,index)=>{const Icon=[Code2,Network,Layers3,Database,Cloud][index];return <Reveal key={category.name} className="skill-row"><div className="skill-title"><span className="skill-icon"><Icon size={23} strokeWidth={1}/></span><div><h3>{category.name}</h3><p>{category.subtitle}</p></div></div><div className="tags skill-tags">{category.items.map(skill=><span key={skill}>{skill}</span>)}</div></Reveal>;})}</div></section>
  <section tabIndex={-1} id="contact" className="contact-section container"><div className="contact-orbit" aria-hidden="true"><Compass size={42} strokeWidth={0.65}/></div><span className="eyebrow">THE NEXT EXPEDITION</span><h2>Contact<span>.</span></h2><p>{profile.contact}</p><a className="button primary" href={profile.email}>Get in touch <Mail size={17}/></a><div className="contact-socials"><a href={profile.github} target="_blank" rel="noreferrer">GitHub <ArrowUpRight size={14}/></a><a href={profile.linkedin} target="_blank" rel="noreferrer">LinkedIn <ArrowUpRight size={14}/></a></div></section></main><footer className="container footer"><a className="brand" href="#home"><Compass size={20} strokeWidth={1}/><span>NM.</span></a><p>© {new Date().getFullYear()} Nicholas McCracken</p><a href="#home">Back to the map <ArrowRight size={14}/></a></footer></>;
}
